import React from 'react';
import PostView from '.';

export default {
  title: 'SDK Connected/Social/Pages',
};

export const SDKPostView = (props) => <PostView {...props} />;

SDKPostView.storyName = 'Post View';
